# Binaries
/dbferry
/dist/
*.test

# Go
/vendor/
*.out

# Local secrets / dev DSN files — never commit
*.dsn
.env

# age identities / private keys — never commit (an AGE-SECRET-KEY is a secret)
*.agekey
age-identity*.txt
dbferry-identity*.txt
/path

# Integration stand: throwaway age identity, generated by `make stand-up`
/test/integration/.stand/
.DS_Store
